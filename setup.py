from setuptools import setup, find_packages

setup(
    name="demo",
    version="0.1",
    packages=find_packages(),
    author="Kazeko Akechi",
    description="These packages provide some tools especially decorators for testing."
)
